# Application Configuration
PORT=52983
HOST=0.0.0.0
GIN_MODE=release

# Timezone
TZ=Asia/Jakarta

# Docker Configuration
COMPOSE_PROJECT_NAME=dramaqu

# Note: Domain will be detected dynamically from request headers
# No need to set DOMAIN variable - supports wildcard domains